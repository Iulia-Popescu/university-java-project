package Event.MusicEvent;

import Event.Event;

public class MusicEvent extends Event {
    public MusicEvent(String name, String date, int normalTickets, int VIPTickets) {
        super(name, date, normalTickets, VIPTickets);
    }
}
