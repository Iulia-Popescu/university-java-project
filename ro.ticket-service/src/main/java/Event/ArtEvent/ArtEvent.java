package Event.ArtEvent;

import Event.Event;

public class ArtEvent extends Event {
    public ArtEvent(String name, String date, int normalTickets, int VIPTickets) {
        super(name, date, normalTickets, VIPTickets);
    }
}
